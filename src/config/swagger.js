import swaggerJsdoc from 'swagger-jsdoc';

const options = {
  definition: {
    openapi: '3.0.0',
    info: {
      title: 'Nirmal Carbon API',
      version: '1.0.0',
      description: 'Indian carbon credit marketplace API. All monetary values in INR (₹). Authenticate using the Authorize button with your JWT access token.',
    },
    servers: [
      {
        url: 'http://localhost:5000',
        description: 'Development Server',
      },
    ],
    components: {
      securitySchemes: {
        bearerAuth: {
          type: 'http',
          scheme: 'bearer',
          bearerFormat: 'JWT',
          description: 'Paste your access token here',
        },
      },
    },
    security: [
      {
        bearerAuth: [],
      },
    ],
  },
  apis: ['./src/routes/*.js', './src/routes/**/*.js'],
};

export const swaggerSpec = swaggerJsdoc(options);

export default swaggerSpec;
